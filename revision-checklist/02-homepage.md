# 2. Homepage (`/`)

- [x] **Remove the intro block directly under the hero** (the three short text pieces). `(0:16:41–0:16:48)`
- [x] **Stappenplan: keep its position but make it much shorter** — implemented as 3 compact side-by-side steps, stacking at tablet/mobile widths. `(0:15–0:16)`
- [x] **"Heb je hier regelmatig last van?" block: two columns → three columns**. `(0:17–0:23)`
  1. "Situaties waarbij coaching kan passen"
  2. "Wat je hierbij kunt ervaren"
  3. **"Wil jij:"** with the requested outcomes. The medication outcome uses the compliant wording **"minder afhankelijk van medicatie"**.
- [x] **Block below that: make compact, direct and sales-oriented** — implemented with "Spreken deze punten je aan?", the personal-trajectory next step and the gezondheidscheck CTA. `(0:23–0:24)`
- [x] **Remove the repeated health-check CTA inside the hero** — the primary hero button is removed; the navbar CTA remains. The standalone WhatsApp action remains per the separate decision below. `(0:24–0:28)`
- [x] **Remove "Den Bosch" from the hero/H1** — retained in metadata/service-area context for SEO. `(0:25:41, 0:45:34)`
- [x] **Hero USP row: replace the duplicated third badge** — implemented as the medically safe **"Resultaatgericht en persoonlijk"**. The requested "Gegarandeerd resultaat" was deliberately not used; see [10-compliance-flags.md](10-compliance-flags.md). `(0:29–0:31)`
- [x] **Shorten the personal story** and link it through to `/over-astrid/`. `(0:14:49)`
- [x] Keep the WhatsApp "Liever eerst appen?" CTA. `(0:28–0:29)`
- [x] Make the layout calmer/less busy where possible — compact process, shorter story, removed hero form/duplicate CTA and simplified sales callout. `(0:32)`
- [ ] Unclear item: "dat andere stuk dat iets hoger zit, dat mag lager" — still blocked because the referenced written feedback/section identification is not in the workspace. `(0:16:32)`
+
